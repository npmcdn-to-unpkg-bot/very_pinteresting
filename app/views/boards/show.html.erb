<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @board.user_id %>
</p>

<p>
  <strong>Title:</strong>
  <%= @board.title %>
</p>

<%= link_to 'Edit', edit_board_path(@board) %> |
<%= link_to 'Back', boards_path %>
